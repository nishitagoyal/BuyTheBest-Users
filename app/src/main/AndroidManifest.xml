<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.something.groceryapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/bb_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/bb_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.GroceryApp"
        tools:replace="android:icon,android:theme">
        <activity android:name=".activity.OrdersPlacedActivity"></activity>
        <activity
            android:name=".activity.ConfirmationActivity"
            android:theme="@style/Theme.GroceryApp.NoActionBar" />
        <activity
            android:name=".activity.SplashActivity"
            android:theme="@style/Theme.GroceryApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ItemActivity" />
        <activity
            android:name=".activity.RegisterActivity"
            android:theme="@style/Theme.GroceryApp.NoActionBar" />
        <activity
            android:name=".activity.LoginActivity"
            android:theme="@style/Theme.GroceryApp.NoActionBar" />
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>